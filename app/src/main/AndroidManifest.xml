<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk tools:overrideLibrary="org.lucasr.twowayview, org.lucasr.twowayview.widget, jp.wasabeef.recyclerview" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwipeLayout"
        tools:targetApi="31">
        <activity
            android:name="com.dargoz.swipe.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListViewExample"
            android:exported="false"/>
        <activity android:name=".GridViewExample"
            android:exported="false"/>
        <activity android:name=".RecyclerViewExample"
            android:exported="false"/>
        <activity android:name="com.dargoz.swipe.NestedExample"
            android:exported="true"/>
    </application>

</manifest>
